# Angular8Progressiveapp
# Node version 10 <
# npm install -g @angular-cli
# npm install -g firebase-tool
# npm serve-o
# ng build --prod && firebase deploy // Production build as we deploy on firebase google 